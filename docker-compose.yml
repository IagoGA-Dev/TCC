# TODO: Verificar posteriormente o motivo do docker do backend não conseguir acessar o banco de dados.
# TODO: Verificar razão do nodemon não funcionar com o docker. ^ pode estar relacionado com o erro acima.
version: "3.8"

services:
  backend:
    build: ./Backend
    command: yarn start
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    depends_on:
      - db
  # ? Não sei o motivo do erro. Consigo acessar o banco de dados normalmente pelo terminal e pelo dbeaver.
  db:
    image: mariadb
    environment:
      MARIADB_ROOT_PASSWORD: 123
      MARIADB_DATABASE: tcc
      MARIADB_USER: void
      MARIADB_PASSWORD: 123
    volumes:
      - data:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  data:

networks:
  default: